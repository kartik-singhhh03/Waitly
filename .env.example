# ================================================
# FRONTEND ENVIRONMENT VARIABLES (Vite)
# ================================================
# Copy this to .env.local for local development
# For Vercel deployment, set these in Vercel dashboard

# API URL for frontend
# LOCAL: http://localhost:3001
# PRODUCTION: Leave empty or omit (uses relative paths /api/*)
# VITE_API_URL=

# ================================================
# IMPORTANT: Frontend env vars MUST start with VITE_
# They are exposed to the browser, so NEVER put secrets here!
# ================================================
